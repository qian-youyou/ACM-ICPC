#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int main(){
    ll n, k;
    while(cin >> n >> k){
        ll tmp;
        cin >> tmp;
        if(tmp>0&&tmp<pow(2,n)){
            cout<<'='<<endl;
        }
    }
    return 0;
}
